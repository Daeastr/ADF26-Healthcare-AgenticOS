# Smart Launch Gateway example configuration

# Port for the smart-launch-gateway server
PORT=3000

# Express session secret
SESSION_SECRET=change-me

# SMART client configuration
CLIENT_ID=smart-launch-gateway

# Where to mint the OS token (kernel-gateway)
KERNEL_TOKEN_ENDPOINT=http://localhost:8788/auth/mint

# Optional defaults
SCOPE=launch openid fhirUser
POU=smart
