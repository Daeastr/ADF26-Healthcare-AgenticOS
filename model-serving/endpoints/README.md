# Endpoints (scaffold)

Define HTTP/gRPC endpoints for:
- LLM completion/chat
- Classification
- Ranking

All endpoints must:
- Authenticate callers
- Log usage events (redacted)
- Return structured outputs
