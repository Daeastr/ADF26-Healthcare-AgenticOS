# Model Serving (scaffold)

Model endpoints and adapters live here.

Goals:
- Provide versioned inference endpoints
- Ensure reproducibility (model + prompt/template versions)
- Emit usage events to kernel
