# Kernel Gateway (token mint stub)
PORT=8788
TOKEN_SIGNING_SECRET=dev-secret-change-me

# Optional defaults
TOKEN_ISSUER=http://localhost:8788
TOKEN_AUDIENCE_DEFAULT=smart-launch-gateway
TOKEN_TTL_SECONDS=3600
